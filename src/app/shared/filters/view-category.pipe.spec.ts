import { ViewCategoryPipe } from './view-category.pipe';

describe('ViewCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new ViewCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
